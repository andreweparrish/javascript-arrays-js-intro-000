var chocolateBars = ['snickers', 'hundred grand', 'kitkat', 'skittles'];

var addElementToBeginningOfArray(chocolateBars, addElement) {
  var moreChocolate = chocolateBars.shift(addElement);
  return moreChocolate
}
